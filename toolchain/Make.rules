#
# Output dist version
#
.phony: distversion
distversion:
	@echo $(VERSION)

#
# Easy way to build unit tests without running them
# 
.phony: tests
tests: $(check_PROGRAMS)

#
# rule for compiling aesop .ae files
#
.ae.o: 
	$(AM_V_GEN)AE_BLOCKING_PARSER="$(AE_BLOCKING_PARSER)" $(AECC) -o $@ $< -- $(CFLAGS) $(AM_CPPFLAGS) $(CPPFLAGS)

.hae.h:
	$(AM_V_GEN)AE_BLOCKING_PARSER="$(AE_BLOCKING_PARSER)" $(AECC) -o $@ $< -j -- $(CFLAGS) $(AM_CPPFLAGS) $(CPPFLAGS)

#
# rules for .ae dependencies
#
dirname = $(patsubst %/,%,$(dir $(1)))

# TODO: not sure why the mkdir commands are needed below; fix this

%.ae.d: %.ae
	@mkdir -p $(call dirname,$@)
	$(AM_V_GEN)CC="$(CC)" CFLAGS="$(CFLAGS)" $(srcdir)/maint/depend-ae.sh $(call dirname,$@) $< > $@

%.hae.d: %.hae
	@mkdir -p $(call dirname,$@)
	$(AM_V_GEN)CC="$(CC)" CFLAGS="$(CFLAGS)" $(srcdir)/maint/depend-ae.sh $(call dirname,$@) $< > $@

